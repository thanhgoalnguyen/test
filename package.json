{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"sass-watch": "sass style.scss style.css --watch",
	"server": "npx browser-sync start --server --files '*.html, *.css'",
  	"dev": "npm run sass-watch & npm run server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.97.2"
  }
}
